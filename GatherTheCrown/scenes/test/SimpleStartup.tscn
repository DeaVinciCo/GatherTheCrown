[gd_scene load_steps=2 format=3 uid="uid://dqjpkofx9t95wfhf"]

[sub_resource type="GDScript" id="GDScript_simple"]
script = """
extends Node

func _ready() -> void:
	print("\\n=== SIMPLE STARTUP TEST ===")
	print("[Startup] Root scene loaded")
	await get_tree().process_frame
	print("[Startup] Process frame complete")
	print("[Startup] This scene has no complex nodes")
	print("[Startup] If you see this, the bootstrap worked!")
"""

[node name="SimpleStartup" type="Node"]
script = SubResource("GDScript_simple")
